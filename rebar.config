% -*- mode: erlang -*-

% Options to pass into compiler during compile-time.
{erl_opts, [ debug_info,      % Compile with Debug information for now.
             fail_on_warrning % We don't even want to have warnings.
           ]}.
